plugins {
    id 'nebula.release' version '4.1.0'
    id 'nebula.os-package' version '2.2.6' apply false
    id 'maven'
}

nebulaRelease { 
    // need this for gitflow release names
    addReleaseBranchPattern(/release\/\d+(\.\d+)+/)
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.2.1'
}
